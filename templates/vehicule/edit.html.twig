{% extends 'base.html.twig' %}

{% block title %}Edit Vehicule{% endblock %}

{% block body %}
    <h1>Edit Vehicule</h1>

    {{ include('vehicule/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_vehicule_index') }}">back to list</a>

    {# {% if dateToday < endReservation %}
        <p>Le véhicule ne peut pas encore etre supprimé car une reservation est encore prévu et fini le {{ endReservation}}</p>
    {% else %} #}
        {{ include('vehicule/_delete_form.html.twig') }}
    {# {% endif %} #}
    
    
{% endblock %}
